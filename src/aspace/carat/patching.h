#include <nautilus/nautilus.h>


// need to change definition when merge with KARAT
int nk_carat_move_region(void *region_start, void *new_region_start, uint64_t region_length) {
    return 0;
}

int nk_carat_move_allocations(void *allocation_to_move, void *allocation_target) {
    return 0;
}
